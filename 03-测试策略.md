# 03 · 测试策略

> **层级**：L2 规范 ｜ **前置阅读**：[02-工程规范](02-工程规范.md) ｜ **何时读我**：编写或评审任何测试前；怀疑测试不充分时
>
> 核心立场：**测试是对抗性的**。测试的目的不是证明代码对，而是让错误无处藏身。
> 上一版"测试不严谨、低级错误多"的根因，就是测试在写"顺利通过"而不是"全力抓错"。

## 一、测试金字塔

| 层级 | 名称 | 工具 | 抓什么 | 门禁 |
|---|---|---|---|---|
| L0 | 静态检查 | ruff + mypy `--strict` | 类型错误、未定义变量、明显坏味道 | CI 必过 |
| L1 | 单元测试 | pytest | 函数/模型行为，边界条件 | CI 必过 |
| L2 | 数据契约测试 | pytest + 黄金样本 | 数据源格式漂移、解析错误 | CI 必过 |
| L3 | 属性测试 | hypothesis | 开发者没想到的边界（随机轰炸） | CI 必过 |
| L4 | 回测专项测试 | pytest（自定义框架） | 未来函数、不可复现、成本建模错误 | 策略合入必过 |

覆盖率门禁：核心计算模块（`quality`/`backtest`/`strategy`/`tasks`）≥ 90%，整体 ≥ 80%。清单变更需用户批准。

## 二、各层细则

### L1 单元测试

- **交叉验证原则**：指标计算（MA/MACD/RSI 等）必须与 TA-Lib 对照，误差 < 1e-8。禁止"用自己的函数验证自己"。
- 每个 pydantic 模型的字段校验：合法值通过 + 每类非法值各一个反例。

### L2 黄金样本测试（数据源的生命线）

- 每个数据源适配器必须配黄金样本：固定日期的**真实原始响应**（快照存档）+ 期望解析结果。
- CI 中重放解析，与期望结果 diff，任何字段漂移立即测红。
- **更新黄金样本是被允许但高敏的操作**：必须人工确认 diff 合理（源真的变了），并在提交信息中说明。这是防"源悄悄变了没人知道"的关卡。

### L3 属性测试

对每个核心不变量写 hypothesis 测试（日常 CI 每条 ≥ 1 万例；深度档 ≥ 10 万例，每周 + Step 验收前运行）：

- OHLC 不变量：`high ≥ max(open, close) ≥ min(open, close) ≥ low`，`volume ≥ 0`
- 复权恒等式：前复权 → 后复权 → 不复权 往返变换后数值还原
- 门禁判定完备性：任何违反不变量的数据必须被门禁拦截（生成脏数据验证拦截率 100%）
- 时间序列：按日重采样后再还原，成交额守恒

### L4 回测专项测试（量化项目的杀手锏）

#### 4.1 确定性测试
同数据、同参数、同种子，跑 3 次，净值序列逐位相等。浮点不确定来源（并行、字典序、未设种子）一经发现立即修复。

#### 4.2 截断一致性测试（防未来函数）

> 对每个策略、每个历史时点 T（抽样 ≥ 50 个时点）：
> 用**截断到 T 时刻**的数据跑出的 T 时刻信号，
> 必须与用**全量数据**跑出的 T 时刻信号完全一致。
> 不一致 = 存在前视偏差（Lookahead Bias），禁止合入。

实现要点：信号计算函数必须纯函数化（只依赖输入数据窗口），测试框架自动构造截断数据集。任何策略想合入，此测试自动生成并必须通过。

#### 4.3 成本敏感性测试
佣金/滑点参数上下浮动 50%，策略结论（正期望与否）不应反转。反转 = 策略利润来自成本假设的乐观误差，亮红灯并要求人工归因。

#### 4.4 不可成交约束测试
构造跌停、停牌、一字涨停样本，回测必须**不产生成交**。能在跌停板上买入的回测引擎是骗子引擎。

#### 4.5 回测台账（实验留痕；持久化实现见 [09](09-任务流水线与回测台账.md) 第五节）
- 每次回测运行必须登记：运行时间、策略版本/参数、数据窗口、区间、结果摘要（净值/胜率/回撤等）
- 策略报告必须引用台账编号（backtest_id），并显著披露"**这是该策略的第 N 次回测**"——反复调参只报最优 = 数据窥探，台账让多次试验无处隐藏
- "测试段只许用一次"由台账强制执行：测试段结果一经查看，该策略版本不得再调参重测测试段

#### 4.6 幸存者偏差防御（含退市股）
- 回测样本必须按"历史当时"的全市场成分还原，**包含已退市股票**；排除退市股 = 用事后的存活信息筛选样本 = 变相未来函数，结果必然虚高
- 数据源不覆盖退市股时，报告必须显著声明偏差范围与方向

## 三、测试粒度原则

| 必须测 | 建议测 | 不强制测 |
|---|---|---|
| 跨模块契约（pydantic 模型） | 内部辅助函数 | 纯转发/无逻辑胶水代码 |
| 门禁规则（每条规则正反两例） | 配置解析 | 第三方库已保证的行为 |
| 策略信号函数 | 报告渲染格式 | |
| 回测引擎核心路径 | | |

**已决（2026-09-17）**：
1. 属性测试规模：日常 CI 每条 ≥ 1 万例（快速档）；深度档 ≥ 10 万例，每周及 Step 验收前在家里 WSL2 运行
2. 黄金样本更新：agent 可提议并附 diff，**用户批准后方可入库**
3. 覆盖率阈值 90%/80% 确认；核心模块清单（`quality`/`backtest`/`strategy`/`tasks`）维护在 02-DoD，变更需用户批准
